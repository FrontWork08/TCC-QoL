{
  "rules": {
    ".read": false,
    ".write": false,
    "users": {
      ".read": "auth != null && auth.token.admin === true",
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || auth.token.admin === true)",
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "presence": {
      "$uid": {
        ".read": "auth != null",
        ".write": "auth != null && auth.uid === $uid",
        ".validate": "newData.hasChildren(['name', 'uid', 'online'])",
        "name": { ".validate": "newData.isString() && newData.val().length >= 1 && newData.val().length <= 80" },
        "uid": { ".validate": "newData.isString() && newData.val() === auth.uid" },
        "online": { ".validate": "newData.isBoolean()" }
      }
    },
    "friends": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || auth.token.admin === true)",
        "$friendUid": {
          ".write": "auth != null && auth.uid === $uid",
          ".validate": "newData.hasChildren(['uid', 'name']) && newData.child('uid').val() === $friendUid && newData.child('name').isString() && newData.child('name').val().length <= 80"
        }
      }
    },
    "rooms": {
      "comunidade": {
        "messages": {
          ".read": "auth != null && (auth.token.admin === true || (query.limitToLast != null && query.limitToLast <= 100))",
          "$messageId": {
            ".write": "auth != null && (auth.token.admin === true || (!data.exists() ? newData.child('uid').val() === auth.uid : data.child('uid').val() === auth.uid))",
            ".validate": "newData.hasChildren(['uid', 'name', 'text', 'ts']) && newData.child('uid').isString() && newData.child('uid').val() === auth.uid && newData.child('name').isString() && newData.child('name').val().length >= 1 && newData.child('name').val().length <= 80 && newData.child('text').isString() && newData.child('text').val().length > 0 && newData.child('text').val().length <= 500 && newData.child('ts').isNumber()",
            "text": { ".validate": "newData.isString() && !newData.val().matches(/(filh[oa] da puta|vai se foder|vai tomar no cu|pau no cu|fdp|arrombad[oa]|retardad[oa])/i)" },
            "moderation": { ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 40)" }
          }
        }
      },
      "$roomId": {
        "messages": {
          ".read": "auth != null && (auth.token.admin === true || (($roomId.beginsWith(auth.uid + '_') || $roomId.endsWith('_' + auth.uid)) && query.limitToLast != null && query.limitToLast <= 100))",
          "$messageId": {
            ".write": "auth != null && (auth.token.admin === true || (($roomId.beginsWith(auth.uid + '_') || $roomId.endsWith('_' + auth.uid)) && (!data.exists() ? newData.child('uid').val() === auth.uid : data.child('uid').val() === auth.uid)))",
            ".validate": "newData.hasChildren(['uid', 'name', 'text', 'ts']) && newData.child('uid').isString() && newData.child('uid').val() === auth.uid && newData.child('name').isString() && newData.child('name').val().length >= 1 && newData.child('name').val().length <= 80 && newData.child('text').isString() && newData.child('text').val().length > 0 && newData.child('text').val().length <= 500 && newData.child('ts').isNumber()"
          }
        }
      }
    }
  }
}
