{
  "name": "vitaia",
  "version": "1.0.0",
  "description": "VitaIA — assistente de qualidade de vida com IA",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check api/ai.js && node --check api/status.js && node --check firebase.js && node --check firebase-auth-bridge.js && node --check community-global.js && node --check app.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
